ui/node_modules/
ui/dev-dist/
# Local install artifacts Crew writes into the installed app dir
# (per the official kirocrew-app-dev skill) — never commit these.
.app_secret
/build
/release-info
app-crons.json
installed.json
data/
.omc/
.env
.DS_Store
# Harness-generated wshobson/agents artifacts (local installs only — see docs/ai-resource-intake.md)
.codex/
.opencode/
.antigravity/
.copilot/
# Personal planning docs — this repo is public; these must never be committed.
GAMEPLAN*.md
GRILL-LOG*.md
# Real work data pasted into deliverable kits stays local-only.
deliverables/**/*-filled.*
deliverables/**/local/
# Default-deny spreadsheets under deliverables/ — only blank *-template.xlsx
# files are committable; any pasted/derived data file is ignored by default.
deliverables/**/*.xlsx
deliverables/**/*.xlsm
deliverables/**/*.csv
!deliverables/**/*-template.xlsx
