# Grill log — 2026-08-26

Running record of the `/grilling` session: questions, answers, recommendations, and how the
understanding changed. Append-only. Newest section at the bottom.

> ⚠️ **This repo is public on GitHub.** This file holds Boeing work context. Add it to
> `.gitignore` or move it out of the repo — do not commit it.

---

## ⭐ FINAL ALIGNMENT — read this first

### The cross-cutting finding

**External clocks drive execution here; importance does not.** Every deliverable with a fixed
time and an audience gets done. Every one without has stalled, regardless of how important it is.
OTB has 10am and people in a room → happening tonight. SCMODS has a vague "December" → six weeks
untouched. SEDW has no date at all → stalled longest. Taskmaster Pro has intrinsic pull and no
deadline → 36 commits in three days.

Operational consequence: **any important work without an external clock must have one
manufactured, or it will not happen.** This is the single most transferable output of the session.

### State of play

| # | Deliverable | Current state | Next step | Urgency / Importance |
|---|---|---|---|---|
| 1 | **OTB chatbot test-case diagnosis** | Erin's ~20-row prompt/response sheet **unread**. Analysis at zero. Dashboard exists; state deliberately unassessed — it is context, not the deliverable. | Steps 1–7 below, ~72 min, **tonight** | 🔴 **Highest.** Only item with a hard clock: 10am in person, Review + Working session |
| 2 | **GitLab `Kiro_BI` / `Kiro`** | **Cause established:** Collin and a coworker were building the same thing in parallel, unaware. Not an architectural split — genuine duplicate work. Consequence: **not unilaterally Collin's to merge.** "Tomorrow morning" is almost certainly self-imposed. | **10-minute conversation with the coworker** about which repo survives — before any merge. README headers only if the conversation slips. | 🟡 Low-medium. Real consolidation need, but blocked on a person, not on effort |
| 3 | **SEDW migration** | Ryan contacted **today** — confirmed still urgent, still blocking another team's SQL Server decom. June target blown by ~2 months. **No date now.** Pipeline had no owner; Collin owns it. Scope still undefined — that is the real gap. | **Clock manufactured: scope and plan by Wednesday 2026-09-09** (2 weeks, Collin's call). Scope + plan only — *not* the migration, which cannot be sized yet. Tell Ryan tomorrow after the meeting. | 🟠 High importance. **Now has a self-set clock** — the first time in six weeks |
| 4 | **SCMODS Alation audit** | Not urgent — December confirmed. Four Urgent Linear tickets driven by a **stale 2026-08-07 target**; fix the dates. **Alation work is not stalled** — it is actively pointed at the datasets being set up in **Skylar/SCAI**, which leverage Alation for LLM context. | Fix the stale dates. Otherwise leave it. | 🟢 Low |
| 5 | **Taskmaster Pro** | `0.2.0` prototype. ~3,260 lines of app vs ~5,900 lines of scaffolding. Core capability (app-owned slots) deferred. | Two-week rule: **no commits to `.kiro/`, `.github/`, `.cursor/`, or `*.md`.** Product code only | ⚪ None. This is the substitute activity, not a deliverable |

**Unresolved:** Collin's original enumeration skipped a "#3" — never established whether it exists.

---

## Deliverables in scope (as stated by Collin, 2026-08-26)

Ordered by deadline, not importance.

| # | Deliverable | Deadline | Notes |
|---|---|---|---|
| 4 | Supplier OTB visibility dashboard + AI chat / LLM test cases | **Tomorrow, in-person 10am** | The live one |
| 5 | Merge or distinguish GitLabs `Kiro_BI` and `Kiro` | **Tomorrow morning** | |
| 1 | SEDW migration | Newly urgent | A SQL Server decom is blocked on the SEDW pipeline still running on it |
| 2 | SCMODS Alation audit | **December** | Linear's `2026-08-07` target is **stale** and has been manufacturing false urgency |

Item "3" was skipped in Collin's enumeration — unresolved whether it exists.

### Tracker reliability

Linear and GitHub were pulled directly. Collin flagged them as possibly stale; the SCMODS date
proves that out. **Treat tracker state as a hint, not a fact.** Boeing-side artifacts (OTB
dashboard, both GitLabs, Erin's spreadsheet) are unreachable from this workspace — current state
on those must come from Collin.

---

## Q1 — What is the 10am meeting actually for?

**Recommended:** (b) Review — the most common shape for an in-person BI checkpoint and the most
forgiving. A narrated rough edge reads as competence in a review and as failure in a demo.

**Answer: (b) Review AND (d) Working session.**

Substance Collin wants to bring: Erin populated a spreadsheet with **test prompts and the
chatbot's responses**. Collin wants to come back with *actionable ways to improve the results*
using the available levers.

### The three levers (Collin's framing)

1. **Chatbot system prompt** — load it up / steer it better as it responds to each interaction.
2. **Alation Data Catalog quality** for the SQL Server view the chatbot ingests — better column
   definitions, documented nuances, enumerations, so the LLM can infer what each column means.
3. **The SQL Server View itself** — change granularity, time span, or column set if that serves
   the chatbot better.

**Explicitly out of scope:** the underlying data *schema* is fixed — sourced from an external
supplier. Can be advocated for, but it is not immediate or strategically interesting enough for
tomorrow.

### 🔑 Cross-link discovered

**Lever 2 is the SCMODS Alation audit.** The "December, not urgent" backlog item is the same body
of work as the highest-leverage fix for tomorrow's chatbot problem. Improving column definitions
and enumerations in the catalog *is* the Alation audit, scoped to the OTB view.

Implication: a six-week-stalled backlog item can be reframed as a demo asset. Tomorrow's meeting
supplies the urgency and the audience that the Alation work has been missing. One narrow slice,
done against the OTB view, serves both deliverables at once.

---

## Q2 — What state is the pass through Erin's spreadsheet in?

**Recommended:** regardless of state, the unit of work is two new columns in Erin's sheet —
`Lever` (1/2/3/none) and `Why` (one line, per response). Turns reading into producing, is the
right artifact for a B+D session, and converts "we should invest in catalog quality" from an
opinion into a count.

**Answer: (a) — not opened, or skimmed only.**

### 🔑 Consequence — tonight's block is decided

The core input to a 10am meeting has not been read, ~17 hours out. That settles the schedule:

- **Tonight = the spreadsheet analysis. The whole block.** Nothing else.
- **OTB dashboard polish → deferred.** It is context for the meeting, not the deliverable. The
  deliverable is the diagnosis of the chatbot's responses.
- **GitLab `Kiro_BI` / `Kiro` → 10-minute README-header version tomorrow morning,** not a merge.
  Merging repos the night before a live session is how you arrive with neither.
- **SEDW → one message to Ryan,** sent before the block opens so it works overnight.

### Note on the grill itself

From here the grill risks becoming the procrastination — it is analysis about work rather than
work. Q3 is deliberately constructed so it can **only** be answered from inside the open
spreadsheet. The interview terminates into the first micro-step by design.

---

## Q3 — How many rows, and how many are bad?

**Pre-committed recommendation (stated before the count was known, so the number couldn't
intimidate):** over ~25 rows → sample the 12–15 worst. Under → do them all.

**Answer: ~20 rows.**

Under threshold. **No sampling. Tag all 20.** At ~3 minutes per row that is ~60 minutes — one
evening block, which is the window Collin already has.

---

## Shared understanding (end of grill)

**The deliverable for 10am is not the dashboard.** It is a diagnosis: every one of Erin's 20
prompt/response pairs tagged with which lever fixes it, plus a tally and a recommended first
move.

**The tally is the argument.** "We should invest in catalog quality" is an opinion. "13 of 20
failures are lever 2" is a finding. The count does the persuading.

**Lever 2 carries a credential.** If catalog quality dominates the tally, Collin can say the
Alation audit is already scoped and running — converting a six-week-stalled backlog item into
evidence of foresight.

### Tonight's micro-steps (per `taskmaster-method`)

| # | Step | Time | Done when |
|---|---|---|---|
| 1 | Open Erin's sheet, add `Lever` and `Why` columns, save | 5 min | Columns exist, file saved |
| 2 | First pass — gut-call a lever number on all 20 rows, no prose | 15 min | Zero blank `Lever` cells |
| 3 | Second pass — one line of `Why` per row | 20 min | Zero blank `Why` cells |
| 4 | Tally the counts per lever | 2 min | Three numbers written down |
| 5 | Write the three-sentence headline: the count, the dominant lever, the first move | 10 min | Three sentences exist |
| 6 | Pick one concrete example per lever to read aloud | 10 min | Three row numbers marked |
| 7 | Paste tally + headline wherever you'll present from | 10 min | Artifact openable at 10am |

~72 minutes. Fits one block.

**Stretch, only after 1–7 are complete:** take a single OTB column, write the improved Alation
definition, and note the better answer it would have produced on a specific failing row. One
before/after is the most persuasive object in the room — but it is optional, and reaching for it
before step 7 is the perfectionism stall wearing a tie.

### 📌 Parked — LLM eval / context-engineering skills

Collin wants to explore whether eval / context-engineering skills could give better scoring and
insight on the chatbot results. **Parked, not cancelled.** Candidate sources:

- `/Users/collinboback/.copilot/installed-plugins/awesome-copilot/context-engineering`
- `/Users/collinboback/.copilot/installed-plugins/awesome-copilot/awesome-copilot`
- `/Users/collinboback/.copilot/installed-plugins/awesome-copilot/skills-for-copilot-studio`
- `/Users/collinboback/.copilot/installed-plugins/awesome-copilot/ai-team-orchestration`

Not reachable from this session — `~/.copilot` is outside the connected folders and would need
directory approval. Deliberately **not** requested tonight.

#### Positions on record

**Claude's recommendation: don't touch it before the meeting.**

- At n=20, a human gut-call beats a scoring rubric. Eval infrastructure pays off at ~200 rows,
  or when the scoring has to be *repeatable* across iterations — neither is true tonight.
- Building a rubric first would replace ~60 minutes of producing with an unbounded stretch of
  tooling. That is the documented failure mode.

**Collin's decision: will give it a shot if time permits.** Recorded as the owner's call, taken
with the above stated.

**Agreed gate — both conditions, no exceptions:**

1. Steps 1–7 are complete (tally written, headline written, artifact openable at 10am), **and**
2. A wall-clock cutoff Collin sets *before* opening the spreadsheet — not one decided later, in
   the moment, while interested.

"If time permits" without a clock is how a 60-minute block becomes 3am. The clock is the whole
guardrail; the exploration itself is fine.

**Better use of the impulse: propose it in the room tomorrow.** "For the next pass I want to
score these systematically rather than by judgment — here's the approach." In a B+D working
session that is a strong contribution, it converts the exploration into a sanctioned work item
with an audience, and it directly serves lever 1 (system-prompt engineering) on the next cycle.

#### ✅ Resolved — the skill already exists, and it prescribes tonight's plan

`.kiro/skills/continuous-prompt-evaluation/SKILL.md` (committed 2026-08-25, also in the
`codex-consolidate-main` worktree). Its **Stage 1 — Diagnose** is tonight's task verbatim:

> - Cluster high-frequency dissatisfaction/quality complaint patterns.
> - For each cluster, identify likely contributing instruction(s) or omissions.
> - Capture at least one concrete conversation example per major cluster.
>
> **Output:** Ranked issue list by frequency/severity. Suspected prompt root cause for each issue.

That is exactly: tag 20 rows → tally → dominant lever → one example per lever. **The parked
exploration is unnecessary — the artifact is built and it agrees with the plan.**

Bonus: **Stages 2–4 (Design / Test / Evaluate) are the pitch for tomorrow.** "Stage 1 is done,
here's the ranked issue list; Stages 2–4 are the next iteration." The proposal is pre-written.

Caveat: Stage 3's cohort machinery (deterministic identity hashing, sample-size gates) is scaled
for Kiro-level traffic. With ~20 supplier test prompts, take the *rubric discipline* — one shared
rubric, evidence from the response text only, ambiguous cases stay neutral — and leave the cohort
apparatus alone.

Other skills present, not relevant tonight: `sql-optimization`,
`sql-server-table-reconciliation`, `find-skills`, `kpi-dashboard-design`, `repo-migration`,
`validate-data`, `validating-database-integrity`, `source-command-gen-report`,
`source-command-seed-data`.

### Deferred, explicitly

- OTB dashboard polish — context, not deliverable
- GitLab `Kiro_BI` / `Kiro` — README-header version tomorrow AM, not a merge
- SEDW — **superseded.** Collin already spoke to Ryan today; Ryan confirmed it is still urgent
  and still blocking the other team's SQL Server decommission. Contact is made — the six-week
  stall is broken. But that is a *status*, not a *scope*: it confirms pressure without defining
  the work. **Next step is now one question, tomorrow, after the 10am meeting:** *"What's the
  target decom date on their side? I want to work backwards from it."* That date is the only
  fact that sets priority for everything after tomorrow.

  **Correction — asking for the date is a dead end.** The original decom target was **June**; it
  has already blown by ~2 months and is now "up in the air" because *no one was supporting the
  pipeline*. The other team has stopped planning around a date. Nobody will supply one.

  **🔑 Root cause of the six-week stall: SEDW has no forcing function.** Pattern across all four
  deliverables — OTB gets done tonight because people are in a room at 10am; SCMODS has a vague
  "December" and has sat; SEDW has nothing and has sat longest. Importance does not drive
  Collin's execution. External clocks do. **SEDW will not get done until Collin supplies the date
  himself** — and the "no one was supporting it" vacuum is precisely the one he just stepped into.

  **Revised next step (tomorrow, after the 10am meeting):** commit to a date for the *scope and
  plan*, not the migration — sizing the migration isn't possible yet, and an unhittable date
  restarts the stall.

  > Ryan — since the June target slipped and there's no date now, I'll own one. I'll have the
  > SEDW scope and a migration plan by [date ~1–2 weeks out]. If that works I'll take it to their
  > team so they can re-plan the decom.

  Makes Collin the person who unstuck a two-month-old ownerless blocker, and manufactures the
  clock the work requires. Does **not** change tonight: no-date-urgent loses to
  10am-with-people-in-a-room.
- SCMODS Alation — December; stale Linear dates to be corrected

